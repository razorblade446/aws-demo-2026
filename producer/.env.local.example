DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=producer

# Kafka broker address — matches the docker-compose kafka service port mapping.
# Change to the EC2 private IP (e.g. 10.0.1.10:9092) when pointing at AWS.
KAFKA_BROKER=localhost:9094
